apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
leaderElection:
  leaderElect: false
profiles:
  - schedulerName: dag-scheduler
    plugins:
      filter:
        enabled:
          - name: DAGConstraints
      score:
        enabled:
          - name: DAGConstraints
